{
  "name": "chatgpt-team-archive",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "node --check apps/extension/background.js && node --check apps/extension/content-script.js && node --check apps/extension/popup.js && node --check apps/extension/archive.js",
    "test": "npm run check",
    "test:legacy": "node apps/companion/test/archive-store.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
